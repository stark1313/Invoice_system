{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "서버 실행",
      "group": { "kind": "build", "isDefault": true },
      "type": "shell",
      "command": "python app.py",
      "options": {
        "cwd": "${workspaceFolder}",
        "env": { "PATH": "${workspaceFolder}/venv/bin:${env:PATH}" }
      },
      "problemMatcher": [],
      "presentation": {
        "reveal": "always",
        "panel": "shared"
      }
    }
  ]
}
